{
  "name": "select-qoute",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx tsc",
    "test:desktop": "npm run build && npx cucumber-js --tags '@desktop'",
    "test:mobile": "npm run build && npx cucumber-js --tags '@mobile'",
    "test": "npm run build && npx cucumber-js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
